// import { Page } from 'playwright/test';

// export async function setGameState(page: Page, state: any) {
//   await page.goto('/');
//   await page.evaluate((newState) => {
//     // attach your store to window for testing if needed
//     window.__TEST_STORE__?.setState(newState);
//   }, state);
// }
